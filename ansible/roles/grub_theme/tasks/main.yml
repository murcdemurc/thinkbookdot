- name: Install GRUB theme files
  become: true
  ansible.builtin.copy:
    src: nord/
    dest: /usr/share/grub/themes/nord/

- name: Regenerate GRUB config
  become: true
  ansible.builtin.command:
    cmd: grub-mkconfig -o /boot/grub/grub.cfg
  changed_when: true